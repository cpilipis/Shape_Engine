#ifndef LOADING
#define LOADING
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "bodies.h"


//reads a single line of an open level file to produce a single static body
body spoutStatic(FILE *, int, int);
 
#endif
